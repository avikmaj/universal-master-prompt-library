# Agent Instructions

This is a model-agnostic universal prompt library. Search `catalog.json` and `upstream/aj-geddes-useful-ai-prompts/PROMPT-INDEX.json`; choose the narrowest matching prompt. Preserve instruction priority, source licenses, privacy and domain safety. Do not invent sources, credentials, calculations, measurements or test results. Do not edit files under `upstream/`; adapt copies under the normalized `prompts/` tree and record source metadata. Run `python3 scripts/validate.py` after changes.
